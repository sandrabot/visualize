ktor {
    deployment {
        port = 41523
        port = ${?PORT}
        shutdownGracePeriod = 0
        shutdownTimeout = 3000
    }
    application {
        modules = [ MainKt.module ]
    }
}
